lotto:
  result-checker:
    config:
      scheduler-cron: "*/7 * * * * *"
  winning-numbers-generator:
    config:
      generator-cron: "*/5 * * * * *"
  jwt:
    config:
      secret: test-secret
      issuer: Lotto Service
      expirationDays: 365

spring:
  cache:
    type: none
    redis:
      time-to-live: PT10S